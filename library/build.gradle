apply plugin: 'maven-publish'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.VitalEngage'
            artifactId = 'vitalengage'
            version = 'v1.0.5'

            artifact file('vitalengage.aar')
            packaging 'aar'
        }
    }
}
